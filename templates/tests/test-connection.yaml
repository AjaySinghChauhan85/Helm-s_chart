apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "life_insurance.fullname" . }}-test-connection"
  labels:
    {{- include "life_insurance.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "life_insurance.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
